<dialog id="pdf_modal" class="modal">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>

    <h3 class="text-lg font-bold"><%= t("after_signup.faqs.pdf_upload_modal.title") %></h3>
    <p class="py-4 text-sm"><%= t("after_signup.faqs.pdf_upload_modal.description") %></p>

    <%= render partial: "after_signup/faqs/pdf_upload_form", locals: { chatbot: chatbot } %>
  </div>
</dialog>
