<%= turbo_stream_from "integrations" %>

<% content_for :sub_navigation do %>
  <%= render "shared/chatbot_header" %>
<% end %>

<h1 class="text-3xl font-bold mb-8">Integraciones</h1>

<!-- Grid layout for integration cards -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <!-- WhatsApp Card -->
  <%= render "chatbots/integrations/waba_card", chatbot: @chatbot %>

  <!-- Instagram Card -->
  <%#= render "chatbots/integrations/ig_card" %>

  <!-- Messenger Card -->
  <%#= render "chatbots/integrations/messenger_card" %>
</div>
