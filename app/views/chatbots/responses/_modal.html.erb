<dialog id="responses_modal" class="modal">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>

    <h3 class="text-lg font-bold"><%= t('.add_new_faq') %></h3>
    <p class="py-4"><%= t('.description', chatbot_name: @chatbot.agent_name) %></p>

    <%= render "chatbots/responses/form", chatbot_response: @response %>
  </div>
</dialog>
