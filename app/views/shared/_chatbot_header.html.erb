<div class="border-b border-gray-200 mb-4">
  <div class="flex px-4">
    <% if @chatbot %>
      <%= link_to "Settings", 
          chatbots_setting_path(@chatbot), 
          class: "py-4 px-4 text-gray-500 font-medium border-b-2 #{current_page?(chatbots_setting_path(@chatbot)) ? 'text-indigo-600 border-indigo-600' : 'border-transparent hover:text-gray-700'}" %>
    <% end %>
  </div>
</div>