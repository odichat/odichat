<nav class="flex items-center justify-between px-6 py-4 bg-white border-b border-b-stone-200 mb-2">
  <div class="flex items-center">
    <%= link_to root_path do %>
      <%= image_tag "logo.svg", alt: "Logo", class: "h-8 w-auto" %>
    <% end %>
  </div>
  
  <div class="hidden md:flex items-center space-x-4">
    <div class="flex items-center space-x-12 mr-8">
      <a href="#" class="text-gray-700 font-medium hover:text-stone-900">Docs</a>
      <a href="#" class="text-gray-700 font-medium hover:text-stone-900">Help</a>
      <a href="#" class="text-gray-700 font-medium hover:text-stone-900">Changelog</a>
    </div>
    
    <% if user_signed_in? %>
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "text-gray-700 font-medium hover:text-stone-900" %>
    <% else %>
      <%= link_to "Log in", new_user_session_path, class: "text-gray-700 font-medium hover:text-stone-900" %>
      <%= link_to "Sign up", new_user_registration_path, class: "bg-black text-white font-medium px-4 py-2 rounded-lg hover:bg-stone-900" %>
    <% end %>
  </div>
  
  <!-- Mobile menu button -->
  <div class="md:hidden">
    <button type="button" class="text-gray-500 hover:text-stone-900 focus:outline-none">
      <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>
</nav>

<!-- Mobile menu, show/hide based on menu state -->
<div class="hidden md:hidden">
  <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
    <a href="#" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-stone-900">Docs</a>
    <a href="#" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-stone-900">Help</a>
    <a href="#" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-stone-900">Changelog</a>
  </div>
  <div class="pt-4 pb-3 border-t border-gray-200">
    <div class="flex items-center px-5">
      <% if user_signed_in? %>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "block px-3 py-2 text-base font-medium text-gray-700 hover:text-stone-900 cursor-pointer" %>
      <% else %>
        <%= link_to "Log in", new_user_session_path, class: "block px-3 py-2 text-base font-medium text-gray-700 hover:text-stone-900" %>
        <%= link_to "Sign up", new_user_registration_path, class: "block px-3 py-2 text-base font-medium bg-black text-white rounded-lg hover:bg-stone-900" %>
      <% end %>
    </div>
  </div>
</div>
